{
    "dependencies": {
        "@matter/main": "0.15.6",
        "http": "^0.0.1-security"
    },
    "devDependencies": {
        "@types/node": "^24.1.0",
        "typescript": "~5.8.3"
    },
    "name": "matter-app",
    "version": "0.1.0",
    "type": "module",
    "scripts": {
        "prepare": "tsc",
        "build": "tsc",
        "app": "tsc && node --enable-source-maps dist/main.js",
        "test": "echo \"Error: no test specified\" && exit 1",
        "clean": "node -e 'fs.rmSync(\"dist\", { recursive: true, force: true })'"
    },
    "description": "A simple on/off device",
    "author": "user.name=SchneiderMaster <user.email=fynn2746@outlook.de>"
}
